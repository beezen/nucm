global.nucmBaseConfig = {};
/**
 * 全局配置
 * @return {Object} 配置
 * @property {Object} fileConfig 文件配置信息
 * @property {Object} registryConfig 源配置信息
 * @property {String} lang 语言
 */
export const baseInitConfig = global.nucmBaseConfig; // 全局 global 对象
